import { BoldParenthesesDirective } from './bold-parentheses.directive';

describe('BoldParenthesesDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldParenthesesDirective();
    expect(directive).toBeTruthy();
  });
});
