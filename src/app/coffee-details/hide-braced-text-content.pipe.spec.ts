import { HideBracedTextContentPipe } from './hide-braced-text-content.pipe';

describe('HideBracedTextContentPipe', () => {
  it('create an instance', () => {
    const pipe = new HideBracedTextContentPipe();
    expect(pipe).toBeTruthy();
  });
});
