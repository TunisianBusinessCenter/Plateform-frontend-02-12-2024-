import { SearchPipePipe } from '../serarch-pipe/search-pipe.pipe';

describe('SearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
